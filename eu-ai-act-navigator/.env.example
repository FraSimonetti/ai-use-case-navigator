# ===========================================
# EU AI Act Navigator - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# ===========================================

# ===========================================
# SECURITY NOTE
# ===========================================
# This application uses a BYOK (Bring Your Own Key) model.
# Users configure their own LLM API keys in the browser.
# No server-side LLM API keys are required or stored.
# API keys are sent directly from browser to LLM provider.

# ===========================================
# BACKEND (FastAPI) Configuration
# ===========================================

# CORS - Allowed origins for API requests
# In development: Leave empty to allow localhost:3000, localhost:3001
# In production: Set to your frontend domain(s), comma-separated
# Example: ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-domain.com
ALLOWED_ORIGINS=

# Development only: Set to true to allow all origins (not recommended for production)
# ALLOW_ALL_ORIGINS=false

# ===========================================
# FRONTEND (Next.js) Configuration
# ===========================================

# Backend API URL
# In development: Leave empty to use http://localhost:8000
# In production: Set to your deployed backend URL
# Example: NEXT_PUBLIC_API_URL=https://your-api.railway.app
NEXT_PUBLIC_API_URL=

# ===========================================
# Deployment Examples
# ===========================================

# --- Vercel (Frontend) ---
# Set NEXT_PUBLIC_API_URL in Vercel dashboard under Environment Variables

# --- Railway (Backend) ---
# Set ALLOWED_ORIGINS in Railway dashboard
# Railway auto-provides PORT variable

# --- Render (Backend) ---
# Set ALLOWED_ORIGINS in Render dashboard
# Render auto-provides PORT variable

# --- Docker ---
# Pass environment variables via docker-compose.yml or -e flags
# Example: docker run -e ALLOWED_ORIGINS=https://myapp.com -p 8000:8000 api

# ===========================================
# Local Development Quick Start
# ===========================================
# 1. Copy this file to .env
# 2. Leave all values empty/default for local development
# 3. Frontend runs on http://localhost:3000
# 4. Backend runs on http://localhost:8000
# 5. Configure your LLM API key in the app's Settings page
